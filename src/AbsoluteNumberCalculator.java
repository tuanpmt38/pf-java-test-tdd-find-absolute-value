public class AbsoluteNumberCalculator {
    public static int findAbsolute(int number) {
        //đánh dấu phương thức này chưa hoàn thiện
        //throw new UnsupportedOperationException();
        //return 0;
        if (number < 0) {
            return -number;
        }
        return number;
    }
}
